import { RegExpDirective } from './reg-exp.directive';

describe('RegExpDirective', () => {
  it('should create an instance', () => {
    const directive = new RegExpDirective();
    expect(directive).toBeTruthy();
  });
});
